

export const TodoPreview = ({ todo }) => {

    // const [check, setChecklist] = useState(null)


    return (
        <section className="todo-preview">
            {todo.title}
        </section>
    )


}